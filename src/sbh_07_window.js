SBH.versions.window = 3; (function () { var P = Packages; var WindowManager = P.android.view.WindowManager; var FrameLayout = P.android.widget.FrameLayout; var Gravity = P.android.view.Gravity; var KeyEvent = P.android.view.KeyEvent; var View = P.android.view.View; var Context = P.android.content.Context; var Runnable = P.java.lang.Runnable; var File = P.java.io.File; var C = SBH.theme.colors; var W = SBH.widgets; var MATCH = W.MATCH; function createController() { var controller = { wm: null, root: null, shell: null, content: null, nav: null, page: 0, attached: false, visible: false, rendering: false, stage: "created", onHidden: null }; var stateFile = new File( SBH.paths.bootstrapDir, "full_ui_window_state.json" ); function checkpoint(stage, extra) { var value = extra || {}; controller.stage = stage; value.schemaVersion = 1; value.stage = stage; value.page = controller.page; value.attached = controller.attached; value.visible = controller.visible; value.moduleSetVersion = SBH.bootstrap.moduleSetVersion; value.updatedAt = SBH.util.now(); try { SBH.files.writeJson(stateFile, value); } catch (ignored) {} } function hideKeyboard() { var imm; try { imm = SBH.ctx.getSystemService( Context.INPUT_METHOD_SERVICE ); if (controller.root !== null) { imm.hideSoftInputFromWindow( controller.root.getWindowToken(), 0 ); controller.root.clearFocus(); } } catch (ignored) {} } controller.buildTopBar = function () { var bar = W.row(); var brand = W.column(); var subtitle; var closeBox = new FrameLayout(SBH.ctx); bar.setPadding( SBH.util.dp(16), SBH.util.dp(10), SBH.util.dp(10), SBH.util.dp(8) ); bar.setBackgroundColor(SBH.util.color(C.bg)); bar.addView(W.brandView()); brand.setPadding(SBH.util.dp(10), 0, 0, 0); brand.addView(W.text("SingBoxHub", 22, C.navy, true)); subtitle = W.text( "ShortX Runtime Console", 11.8, C.secondary, false ); subtitle.setPadding(0, SBH.util.dp(3), 0, 0); brand.addView(subtitle); bar.addView(brand, W.lp(0, W.WRAP, 1)); bar.addView( W.label( "Runtime 未连接", C.orange, C.orangeSoft ), W.margins( W.lp(W.WRAP, SBH.util.dp(30)), 0, 0, 8, 0 ) ); closeBox.setBackground( SBH.theme.rounded(C.surface, 13, C.line, 1) ); closeBox.addView( W.icon("close", 28, C.text), W.fp(MATCH, MATCH, Gravity.CENTER) ); W.click(closeBox, function () { controller.hide(); }); bar.addView( closeBox, W.lp(SBH.util.dp(42), SBH.util.dp(42)) ); return bar; }; controller.buildRoot = function () { var root = new FrameLayout(SBH.ctx); var shell = W.column(); var placeholder = W.text( "正在加载 SingBoxHub 界面...", 14, C.secondary, false ); root.setBackgroundColor(SBH.util.color(C.bg)); root.setFocusable(true); root.setFocusableInTouchMode(true); shell.setBackgroundColor(SBH.util.color(C.bg)); shell.addView( controller.buildTopBar(), W.lp(MATCH, SBH.util.dp(72)) ); controller.content = new FrameLayout(SBH.ctx); placeholder.setGravity(Gravity.CENTER); controller.content.addView( placeholder, W.fp(MATCH, MATCH, Gravity.CENTER) ); shell.addView( controller.content, W.lp(MATCH, 0, 1) ); controller.nav = new FrameLayout(SBH.ctx); shell.addView( controller.nav, W.lp(MATCH, SBH.util.dp(68)) ); root.addView(shell, W.fp(MATCH, MATCH)); controller.shell = shell; root.setOnKeyListener(new JavaAdapter( P.android.view.View.OnKeyListener, { onKey: function (view, keyCode, event) { if (keyCode === KeyEvent.KEYCODE_BACK && event.getAction() === KeyEvent.ACTION_UP) { hideKeyboard(); if (controller.page !== 0) { controller.showPage(0); } else { controller.hide(); } return true; } return false; } } )); return root; }; controller.params = function () { var type = SBH.Build.VERSION.SDK_INT >= 26 ? WindowManager.LayoutParams.TYPE_APPLICATION_OVERLAY : WindowManager.LayoutParams.TYPE_PHONE; var flags = WindowManager.LayoutParams.FLAG_NOT_TOUCH_MODAL; var params = new WindowManager.LayoutParams( MATCH, MATCH, type, flags, P.android.graphics.PixelFormat.TRANSLUCENT ); params.gravity = Gravity.TOP | Gravity.START; params.windowAnimations = 0; params.softInputMode = WindowManager.LayoutParams.SOFT_INPUT_ADJUST_RESIZE | WindowManager.LayoutParams.SOFT_INPUT_STATE_ALWAYS_HIDDEN; params.setTitle("SingBoxHub Full UI"); return params; }; controller.renderNow = function (index) { var pageFactory; var pageView; var navView; if (!controller.attached || controller.rendering || controller.content === null || controller.nav === null) { return; } controller.rendering = true; try { controller.page = Number(index); pageFactory = SBH.navigation.pages[controller.page]; if (!pageFactory) { controller.page = 0; pageFactory = SBH.navigation.pages[0]; } if (!pageFactory) { throw new Error( "Home page factory is not registered" ); } checkpoint("before_page_render", { targetPage: controller.page }); controller.content.removeAllViews(); controller.nav.removeAllViews(); pageView = pageFactory(controller); navView = SBH.navigation.build(controller); controller.content.addView( pageView, W.fp(MATCH, MATCH) ); controller.nav.addView( navView, W.fp(MATCH, MATCH) ); checkpoint("after_page_render", { renderedPage: controller.page }); } catch (error) { try { controller.content.removeAllViews(); controller.content.addView( W.text( "界面构建失败: " + SBH.util.errorText(error), 13, C.coral, false ), W.fp(MATCH, MATCH, Gravity.CENTER) ); } catch (ignoredRenderError) {} SBH.log.error("window.render", error); checkpoint("page_render_failed", { error: SBH.util.errorText(error) }); } finally { controller.rendering = false; } }; controller.showPage = function (index) { hideKeyboard(); SBH.util.runUi(function () { controller.renderNow(index); }); }; controller.open = function () { SBH.util.runUi(function () { var params; try { if (controller.attached && controller.root !== null) { controller.visible = true; controller.root.setVisibility(View.VISIBLE); controller.root.requestFocus(); checkpoint("shown_existing", {}); return; } checkpoint("before_build_shell", {}); controller.wm = SBH.ctx.getSystemService( Context.WINDOW_SERVICE ); controller.root = controller.buildRoot(); params = controller.params(); checkpoint("before_add_view", { type: Number(params.type), flags: Number(params.flags) }); controller.wm.addView( controller.root, params ); controller.attached = true; controller.visible = true; checkpoint("after_add_view", {}); SBH.handler.postDelayed( new JavaAdapter(Runnable, { run: function () { if (!controller.attached || controller.root === null) { return; } controller.renderNow( controller.page ); try { controller.root.requestFocus(); } catch (ignoredFocus) {} } }), 120 ); SBH.log.ok( "window", "Full UI window attached" ); } catch (error) { controller.attached = false; controller.visible = false; checkpoint("open_failed", { error: SBH.util.errorText(error) }); SBH.log.error("window.open", error); try { if (controller.wm !== null && controller.root !== null) { controller.wm.removeView( controller.root ); } } catch (ignoredCleanup) {} controller.root = null; controller.content = null; controller.nav = null; controller.shell = null; } }); }; controller.hide = function () { SBH.util.runUi(function () { var root = controller.root; if (!controller.attached || root === null) { controller.visible = false; return; } hideKeyboard(); checkpoint("before_remove_view", {}); try { controller.wm.removeView(root); } catch (error) { SBH.log.warn( "window", "removeView failed: " + error ); } controller.attached = false; controller.visible = false; controller.root = null; controller.content = null; controller.nav = null; controller.shell = null; checkpoint("after_remove_view", {}); if (controller.onHidden) { try { controller.onHidden(); } catch (ignoredHidden) {} } }); }; controller.show = function () { if (controller.attached) { SBH.util.runUi(function () { if (controller.root !== null) { controller.root.setVisibility( View.VISIBLE ); controller.root.requestFocus(); controller.visible = true; } }); } else { controller.open(); } }; controller.toggle = function () { if (controller.attached) { controller.hide(); } else { controller.show(); } }; controller.stop = function () { controller.hide(); }; controller.status = function () { return { visible: controller.visible, attached: controller.attached, page: controller.page, stage: controller.stage }; }; return controller; } SBH.window = { createController: createController }; }());
